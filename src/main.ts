import './style.css'

// 1. Initial configuration 
// Adding necessary JS functionality later if needed
document.addEventListener('DOMContentLoaded', () => {
    // We will handle specific clicks or interactions here if needed
});

console.log('Twosome Project links loaded.');
